@model Junto.Models.Usuario
@{
	ViewBag.Title = "Gera Token";
}

<section class="content">

	@using (Html.BeginForm("GeraToken", "Home", FormMethod.Post))
	{
		<div></div>
		<div>Gera o token na base de dados</div>
		<div></div>

		@Html.AntiForgeryToken()
		<div class="form-group row">
			@Html.ValidationMessageFor(model => model.strMatricula, "", new { @class = "text-danger" })
			<div class="col-sm-3">
				<label for="Nome">Matricula</label>
				@Html.EditorFor(model => model.strMatricula, new { htmlattributes = new { @class = "form-control form-cnotrol-sm" } })
			</div>
		</div>
		<div class="form-group row">
			<div class="col-sm-1">
				<button type="submit" class="btn btn-primary btn-sm" style="padding-bottom:7px;">Gerar Token</button>
			</div>

		</div>
	}
</section>